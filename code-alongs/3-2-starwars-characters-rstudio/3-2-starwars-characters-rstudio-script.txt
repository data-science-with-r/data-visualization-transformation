Steps to get from 3-2-starwars-characters-rstudio.qmd to the -eov (end-of-video) version:

- Fix the wording of one prompt: "change color the points by gender" becomes
  "change the color of the points by gender".
- Color the points by gender by adding `color = gender` to the aes():
    ggplot(starwars, aes(x = height, y = mass, color = gender)) + ...
- In the next plot, set the points to a fixed color by moving color out of aes() into the
  geom: change geom_point() to geom_point(color = "pink").
- Build the fully annotated scatterplot: map color and shape to gender and add axis/legend
  titles:
    ggplot(starwars, aes(x = height, y = mass, color = gender, shape = gender)) +
      geom_point() +
      labs(
        title = "Weights and heights of Star Wars characters",
        x = "Height (cm)",
        y = "Weight (kg)",
        color = "Gender",
        shape = "Gender"
      )
- Replace the `# add code here` placeholders with the bar-chart code:
    - Bar chart of hair color:
        ggplot(starwars, aes(y = hair_color)) +
          geom_bar()
    - Two stacked bar charts (sex filled by gender, and gender filled by sex):
        ggplot(starwars, aes(x = sex, fill = gender)) +
          geom_bar()

        ggplot(starwars, aes(x = gender, fill = sex)) +
          geom_bar()
    - Faceted bar chart:
        ggplot(starwars, aes(x = gender, fill = sex)) +
          geom_bar() +
          facet_wrap(~hair_color)
